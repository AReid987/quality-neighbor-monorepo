# TypeScript Config Package

This package provides shared TypeScript configuration files for the Quality Neighbor monorepo.

## Purpose
Centralizes and standardizes TypeScript configuration for all projects.

## Usage
Extend or reference these config files in your `tsconfig.json`.

## Setup
No setup required. Reference the configs as needed.
